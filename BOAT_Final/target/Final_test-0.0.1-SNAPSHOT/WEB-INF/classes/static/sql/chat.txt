DROP TABLE CHAT_MESSAGE CASCADE CONSTRAINTS PURGE;

CREATE TABLE CHAT_MESSAGE (
    SENDER VARCHAR2(50) references member(empno) ON DELETE CASCADE,
    RECEIVER VARCHAR2(50) references member(empno) ON DELETE CASCADE,
    CONTENT VARCHAR2(1000),
    CHAT_TIME VARCHAR2(50),
    CHAT_DATE DATE DEFAULT SYSDATE
);

